{
  "name": "asmc4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "preview": "python -m http.server 8000 --directory dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
