# CodeGraph runtime cache (DB, daemon logs, sockets)
.codegraph/

# DeepSeek CLI state
.deepseek/

# Reasonix desktop topic cache
.reasonix/

# Reasonix project config (machine-local permissions)
reasonix.toml

# Agent-local only (machine-specific; never push)
.cursorrules
.opencode/
opencode.json

# Build output
dist/

# Node.js dependencies
node_modules/

# Capacitor Android project (build outputs only)
android/app/build/
android/build/
android/.gradle/
android/local.properties

# Keystore (signing key for release APK)
asmc4.keystore
