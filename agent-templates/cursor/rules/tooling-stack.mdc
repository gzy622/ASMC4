---
description: 多 Agent 工具栈兼容层（ponytail、RTK、Headroom、Waza）— 本地生成，勿提交 Git
alwaysApply: true
---

# 工具栈（多 Agent / Windows）

本文件由 `scripts/setup-agent-local.ps1` 从 `agent-templates/` 生成，**仅本机 `.cursor/rules/`**，不进 Git。

权威顺序：**`AGENTS.md`（项目）> 本文件 > 各 Agent 全局插件**。禁止 Headroom 旧版「所有命令都加 rtk」注入。

## 各 Agent 如何加载（互不覆盖项目规则）

| Agent | 项目入口 | ponytail | RTK / Headroom |
|-------|----------|----------|----------------|
| **Cursor** | `AGENTS.md` + 本目录 `.mdc` | `ponytail.mdc` | Shell 内显式 `rtk`；API 可选 Headroom |
| **Codex App** | `AGENTS.md` | 全局 ponytail 插件（`@ponytail`） | 同左；勿与 Cursor 规则冲突 |
| **OpenCode Desktop** | `AGENTS.md` + 本地 `opencode.json` | `.opencode/plugins/ponytail.mjs` | 同左 |
| **Reasonix Desktop** | `AGENTS.md` + 本地 `reasonix.toml` | 读 AGENTS 第 13–16 条 + ponytail 梯子 | 同左 |

## 分层（互不替代）

| 层 | 工具 | 作用 |
|----|------|------|
| 代码策略 | **ponytail** | 最短可行 diff、YAGNI |
| 终端输出 | **RTK** | 压缩 git/test 等长输出 |
| API 上下文 | **Headroom** | 可选；需本机代理 + 各客户端 Base URL |
| 专项工作流 | **Waza** | `~/.agents/skills/`（hunt/check/think/design/read/write） |
| 过度设计 | **ponytail-*** | 本机 `.cursor/skills/` 或 Codex `@ponytail-review` 等 |

## RTK（与 AGENTS.md 一致）

- 要求 `rtk` 在 PATH（Windows 常见：`%USERPROFILE%\.local\bin\rtk.exe`）。
- **不要**对所有命令无脑加 `rtk`。
- **使用**：`git status/diff/log`（长历史）、`rtk rg`、测试/构建输出可能很长时。
- **不用**：`node build.mjs`、`python verify.py`、短命令、需完整输出、交互式、安装类命令。
- 调试可用裸命令；`rtk proxy <cmd>` 仅跟踪。

## Headroom（可选）

- 要求 `headroom` 在 PATH。
- 启动：`.\scripts\start-headroom-proxy.cmd`（非 WSL）。
- 代理：`http://127.0.0.1:8787`；OpenAI 兼容路径 `/p/ASMC4/v1`，Anthropic `/p/ASMC4`。
- 未配置各客户端 Base URL 时：RTK + ponytail 仍生效，**不算故障**。

## ponytail × ASMC4

- 验证：`node build.mjs`、`python verify.py`（不新加测试框架）。
- 数据流：`events → business → state → saveAppState → render`。
- 小 bug：ponytail；复发/截图：读 **hunt**；合并前：**check**；方案：**think**。
- 刻意简化：`// ponytail: <上限>, <何时升级>`。

## 技能路由

```
小 bug、改一处           → ponytail
回归、崩溃、修了又坏     → hunt
合并前、发版             → check
架构、值不值得做         → think
UI 审美                  → design
读链接                   → read
润色、release notes      → write
过度设计                 → ponytail-review / ponytail-audit
```

hunt 与 ponytail：先读懂再偷懒。

## 已关闭（除非用户明确要求）

browser E2E、GitHub 发布、`~/.agents/skills-disabled/` 下技能。
