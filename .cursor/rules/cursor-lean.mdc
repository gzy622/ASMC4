---
description: Cursor 加速硬开关（单模型直出）
alwaysApply: true
---

# Cursor 加速

- **单线程直出**：不派 Task/explore；不用 browser MCP（预览 `dev.cmd`）。
- **先索引后读**：`CodeGraph.md` + `Grep`（`src/`）→ `Read` offset/limit。
- 项目规则见 `AGENTS.md`。
