---
description: 多 Agent 工具栈（Codex/OpenCode/Headroom）；跨 Agent 协作时 @ 引用
alwaysApply: false
---

# 工具栈（多 Agent / Windows）

权威顺序：**`AGENTS.md` > 本文件 > 全局插件**。禁止 Headroom「所有命令都加 rtk」。

- **RTK**：长 git/rg/测试输出才加；短命令、`node build.mjs`、`python verify.py` 不加。
- **Headroom**（可选）：`.\scripts\start-headroom-proxy.cmd` → Cursor Base URL `http://127.0.0.1:8787/p/ASMC4/v1`
- **技能路由**：小 bug → ponytail；复发 → hunt；新方案 → think；UI → ui；合并前 → check；过度设计 → ponytail-review。
